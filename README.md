# money-manager-with-laravel
clone the repository - git clone https://github.com/aungsoeo/money-manager-with-laravel.git
composer install 
set db name, username, password in .env file
php artisan key:generate
php artisan migrate
php artisan serve
